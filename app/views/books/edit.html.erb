  <%= form_with model: @book, url:book_path(@book.id), method: :patch do |f| %>
  <h4>ショップ名</h4>
      <%= f.text_field :title%>
    <h4></h4>
      <%= f.text_area :body %>
    <%= f.submit '投稿' %>
  <% end %>
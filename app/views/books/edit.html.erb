  <%= form_with model: @book, url:book_path(@book.id), method: :patch do |f| %>
  <h4>Title</h4>
      <%= f.text_field :title%>
    <h4>Opinion</h4>
      <%= f.text_area :body %></br>
    <%= f.submit '投稿' %>
  <% end %>
   <tr>
   <td><%= link_to "show", book_path(@book.id) %></td>
   |
   <tb><%= link_to "Back", books_path(@book.id)%></tb>
<h1>Books#show</h1>
<p>Find me in app/views/books/show.html.erb</p>

<%= render 'users/register', user: @user %>

<%= render 'books/new',book: @book_new %>
</div>

<div class="col-md-8 offset-md-1">
<h1>Book detail</h1>
<table class="table table-hover table-inverse">
<thead>
     <tr>
       <th>Title</th>
       <th>Opinion</th>
       <th colspan="3"></th>
     </tr>
   </thead>
 <tbody>
 <tr>
<td>
 <%=link_to image_tag(@book_list.user.get_profile_image(100,100)) , user_path(@user.id) %></br>

 <%=link_to (@user.name),user_path(@user.id) %>
</td>
<td><%=link_to @book_list.title, book_path(@book_list.id) %></td>

<td><%= @book_list.body %></td></br>

<% if @book_list.user_id == current_user.id %>
<div class="form-group">
<td><%= link_to "Edit", edit_book_path(@book_list.id),class:"btn btn-sm btn-success" %></td>
</div>
<div class="form-group">
<td><%= link_to "Destroy",book_path(@book_list.id) , method: :delete ,"data-confirm" => "本当に消しますか?" ,class:"btn btn-sm btn-danger"%></td>
</tr>
</tbody>
 <%end%>
 </table>
</div>

<h1>Books#index</h1>
<p>Find me in app/views/books/index.html.erb</p>

<div class="col-md-3">

      <h2>User info</h2>
  <!--% user.each do |f|%>-->
<!--% if users.image.attached? %>-->
 <!--%= image_tag users.image, size: "200x200" %>-->
<!--% else %>-->
  <!--%= image_tag 'no_image', size: "200×200" %>-->
<!--% end %>-->
<!--%end%>-->

   <h4>name</h4>
   <!--%= @user.name %>-->
   <h4>introduction</h4>
   <!--%= @user.introduction %>-->
   <!--% end %>-->
   </div>

<!--<tb>%= link_to "Edit", edit_user_path(user.id),class: 'fas fa-user-cog' %></tb>-->
<!--%end%>-->
<h2 class="mt-3">New book</h1>

<%= form_with model: @book,url: '/books/', method: :post do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %><br/>

  <%= f.submit 'Create Book' %>
<% end %>

<table>
 <thead>

 <tr>
  <th>Title</th>
  <th>Opinion</th>
<th colspan="3"></th>
 </tr>
 </thead>

<tbody>
<% @book.each do |book|%>
 <tr>

  <td><%= book.title %></td>
  <td><%= book.body %></td>

   <td><%= link_to "show", book_path(book.id) %></td>

   <td><%= link_to "Edit", edit_book_path(book.id) %></td>

   <td><%= link_to "Destroy", book_path(book.id), method: :delete,"data-confirm" => "Are you sure?" %></td>

 </tr>
<%end%>

</tbody>
</table>
